import { useState } from "react";
import { Eye, Plus, Trash2 } from "lucide-react";
import AdminModal from "./AdminModal";

const API = import.meta.env.VITE_API_URL || "http://localhost:3001";
async function request(path, options = {}) { const response = await fetch(`${API}${path}`, { headers: { "Content-Type": "application/json", "x-admin-token": localStorage.getItem("aviator_admin_token") || "" }, ...options }); if (!response.ok) throw new Error(`Erreur API ${response.status}`); return response.json(); }

export function AdminOrdersPanel({ data, refresh }) {
  const [selected, setSelected] = useState(null);
  const remove = async (order) => { if (!window.confirm(`Supprimer la commande ${order.orderNumber} ?`)) return; await request(`/api/admin/orders/${order.id}`, { method: "DELETE" }); refresh(); };
  return <section className="border border-border bg-background p-5 sm:p-7"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-muted-foreground">Vente</p><h2 className="mt-1 font-display text-2xl font-bold">Commandes</h2></div><div className="mt-6 divide-y divide-border border-y border-border">{data.map((order) => <div key={order.id} className="flex flex-wrap items-center justify-between gap-4 py-4"><div><strong className="font-mono text-sm">{order.orderNumber}</strong><p className="mt-1 text-xs text-muted-foreground">{order.firstName} {order.lastName} · {order.phone} · {order.city}</p></div><div className="flex items-center gap-2"><strong>{(order.total / 100).toLocaleString("fr-FR")} DH</strong><button onClick={() => setSelected(order)} className="border border-border p-2" aria-label="Voir le détail"><Eye className="h-4 w-4" /></button><select value={order.status} onChange={async (event) => { await request(`/api/admin/orders/${order.id}`, { method: "PATCH", body: JSON.stringify({ status: event.target.value }) }); refresh(); }} className="border border-border bg-background px-3 py-2 text-xs"><option value="nouvelle">Nouvelle</option><option value="confirmee">Confirmée</option><option value="preparation">Préparation</option><option value="expediee">Expédiée</option><option value="livree">Livrée</option><option value="annulee">Annulée</option><option value="retour">Retour</option></select><button onClick={() => remove(order)} className="border border-border p-2 text-destructive" aria-label="Supprimer"><Trash2 className="h-4 w-4" /></button></div></div>)}{!data.length && <p className="py-10 text-center text-sm text-muted-foreground">Aucune commande.</p>}</div><AdminModal open={Boolean(selected)} title={`Commande ${selected?.orderNumber || ""}`} onClose={() => setSelected(null)}>{selected && <div className="space-y-3 text-sm"><p><strong>Client :</strong> {selected.firstName} {selected.lastName}</p><p><strong>Téléphone :</strong> {selected.phone}</p><p><strong>Adresse :</strong> {selected.address}, {selected.city}</p><p><strong>Statut :</strong> {selected.status}</p><p><strong>Total :</strong> {(selected.total / 100).toLocaleString("fr-FR")} DH</p><pre className="max-h-48 overflow-auto bg-secondary p-3 text-xs">{JSON.stringify(selected.items, null, 2)}</pre></div>}</AdminModal></section>;
}

export function AdminCouponsPanel({ data, refresh }) {
  const [open, setOpen] = useState(false); const [form, setForm] = useState({ code: "", discount_type: "percentage", value: "10", min_cart: "0" });
  const save = async (event) => { event.preventDefault(); await request("/api/admin/coupons", { method: "POST", body: JSON.stringify(form) }); setOpen(false); setForm({ code: "", discount_type: "percentage", value: "10", min_cart: "0" }); refresh(); };
  const remove = async (coupon) => { if (!window.confirm(`Supprimer ${coupon.code} ?`)) return; await request(`/api/admin/coupons/${coupon.id}`, { method: "DELETE" }); refresh(); };
  return <section className="border border-border bg-background p-5 sm:p-7"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-muted-foreground">Marketing</p><h2 className="mt-1 font-display text-2xl font-bold">Coupons</h2></div><button onClick={() => setOpen(true)} className="flex items-center gap-2 bg-navy px-4 py-3 text-xs font-bold uppercase text-white"><Plus className="h-4 w-4" />Nouveau coupon</button></div><div className="mt-6 divide-y divide-border border-y border-border">{data.map((coupon) => <div key={coupon.id} className="flex items-center justify-between py-4"><div><strong className="font-mono">{coupon.code}</strong><p className="text-xs text-muted-foreground">{coupon.discountType} · utilisé {coupon.usedCount} fois</p></div><div className="flex gap-2"><button onClick={async () => { await request(`/api/admin/coupons/${coupon.id}`, { method: "PATCH", body: JSON.stringify({ active: !coupon.active }) }); refresh(); }} className={`px-3 py-1 text-xs font-bold ${coupon.active ? "bg-accent-lime text-navy" : "bg-muted text-muted-foreground"}`}>{coupon.active ? "Actif" : "Inactif"}</button><button onClick={() => remove(coupon)} className="border border-border p-2 text-destructive" aria-label="Supprimer"><Trash2 className="h-4 w-4" /></button></div></div>)}</div><AdminModal open={open} title="Créer un coupon" onClose={() => setOpen(false)}><form onSubmit={save} className="space-y-4"><input required placeholder="Code promo" value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value.toUpperCase() })} className="w-full border border-border px-4 py-3 text-sm uppercase" /><select value={form.discount_type} onChange={(e) => setForm({ ...form, discount_type: e.target.value })} className="w-full border border-border px-4 py-3 text-sm"><option value="percentage">Pourcentage</option><option value="fixed">Montant fixe</option><option value="free_shipping">Livraison gratuite</option></select><input required type="number" min="0" placeholder="Valeur" value={form.value} onChange={(e) => setForm({ ...form, value: e.target.value })} className="w-full border border-border px-4 py-3 text-sm" /><input type="number" min="0" placeholder="Minimum panier DH" value={form.min_cart} onChange={(e) => setForm({ ...form, min_cart: e.target.value })} className="w-full border border-border px-4 py-3 text-sm" /><div className="flex justify-end gap-3"><button type="button" onClick={() => setOpen(false)} className="border border-border px-4 py-3 text-xs font-bold uppercase">Annuler</button><button className="bg-navy px-4 py-3 text-xs font-bold uppercase text-white">Créer</button></div></form></AdminModal></section>;
}

export function AdminPromotionsPanel({ data, refresh }) { const [open, setOpen] = useState(false); const [form, setForm] = useState({ name: "", slug: "", title_fr: "", title_darija: "", subtitle_fr: "", subtitle_darija: "" }); const save = async (event) => { event.preventDefault(); await request("/api/admin/promotions", { method: "POST", body: JSON.stringify(form) }); setOpen(false); refresh(); }; const remove = async (promotion) => { if (!window.confirm(`Supprimer ${promotion.name} ?`)) return; await request(`/api/admin/promotions/${promotion.id}`, { method: "DELETE" }); refresh(); }; return <section className="border border-border bg-background p-5 sm:p-7"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-muted-foreground">Marketing</p><h2 className="mt-1 font-display text-2xl font-bold">Promotions</h2></div><button onClick={() => setOpen(true)} className="flex items-center gap-2 bg-navy px-4 py-3 text-xs font-bold uppercase text-white"><Plus className="h-4 w-4" />Nouvelle campagne</button></div><div className="mt-6 divide-y divide-border border-y border-border">{data.map((promotion) => <div key={promotion.id} className="flex items-center justify-between py-4"><div><strong>{promotion.name}</strong><p className="text-xs text-muted-foreground">{promotion.titleFr} · {promotion.titleDarija || "Darija à compléter"}</p></div><div className="flex gap-2"><button onClick={async () => { await request(`/api/admin/promotions/${promotion.id}`, { method: "PATCH", body: JSON.stringify({ active: !promotion.active, title_fr: promotion.titleFr, title_darija: promotion.titleDarija, subtitle_fr: promotion.subtitleFr, subtitle_darija: promotion.subtitleDarija, background_color: promotion.backgroundColor, text_color: promotion.textColor, accent_color: promotion.accentColor, image_url: promotion.imageUrl, coupon_code: promotion.couponCode }) }); refresh(); }} className={`px-3 py-1 text-xs font-bold ${promotion.active ? "bg-accent-lime text-navy" : "bg-muted text-muted-foreground"}`}>{promotion.active ? "Active" : "Inactive"}</button><button onClick={() => remove(promotion)} className="border border-border p-2 text-destructive" aria-label="Supprimer"><Trash2 className="h-4 w-4" /></button></div></div>)}</div><AdminModal open={open} title="Créer une campagne" onClose={() => setOpen(false)}><form onSubmit={save} className="space-y-4"><input required placeholder="Nom de campagne" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="w-full border border-border px-4 py-3 text-sm" /><input required placeholder="Slug" value={form.slug} onChange={(e) => setForm({ ...form, slug: e.target.value })} className="w-full border border-border px-4 py-3 text-sm" /><input required placeholder="Titre français" value={form.title_fr} onChange={(e) => setForm({ ...form, title_fr: e.target.value })} className="w-full border border-border px-4 py-3 text-sm" /><input placeholder="Titre darija" dir="rtl" value={form.title_darija} onChange={(e) => setForm({ ...form, title_darija: e.target.value })} className="w-full border border-border px-4 py-3 text-sm" /><textarea placeholder="Sous-titre français" value={form.subtitle_fr} onChange={(e) => setForm({ ...form, subtitle_fr: e.target.value })} className="w-full border border-border px-4 py-3 text-sm" /><textarea placeholder="Sous-titre darija" dir="rtl" value={form.subtitle_darija} onChange={(e) => setForm({ ...form, subtitle_darija: e.target.value })} className="w-full border border-border px-4 py-3 text-sm" /><div className="flex justify-end gap-3"><button type="button" onClick={() => setOpen(false)} className="border border-border px-4 py-3 text-xs font-bold uppercase">Annuler</button><button className="bg-navy px-4 py-3 text-xs font-bold uppercase text-white">Créer</button></div></form></AdminModal></section>; }
